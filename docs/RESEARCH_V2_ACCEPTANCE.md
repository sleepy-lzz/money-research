# 本次软件验收与研究证据边界

日期2026-09-16。这里的测试数来自本次实际运行，不沿用用户旧日志的通过数。所有新增fixture、演示证据和测试JSON都是合成材料，未进入任何真实前瞻数据库。

## 测试结果

| 执行范围 | 结果 | 边界 |
|---|---|---|
| 修改前全量pytest | 299 passed / 42 failed / 3 errors，115.33秒 | 41失败+3错误发生在Parquet依赖；另1个原有intraday夹具TypeError |
| 修改后全量pytest | **371 passed / 41 failed / 3 errors**，129.41秒，退出码1 | 剩余41失败和3错误当前全部停在缺少pyarrow/fastparquet；不是全量通过 |
| 新研究+安全更新+旧复核退休+盘中回归 | **107 passed**，12.11秒 | test_research_v2、test_research_upgrade、test_review_center、test_intraday |
| 新增研究及升级模块单独收集 | 68个参数展开用例 | 59研究+9升级；收集数不证明收益有效 |
| Python compileall | 通过 | 仅语法/字节编译，不代替依赖和运行验证 |
| git diff --check | 通过 | 未声称ruff lint通过，当前缺ruff |
| 源码更新器 | 预检、应用、幂等、恢复、冲突保护、路径保护通过 | 合成源目录与账户哨兵；不意味着用户主机已部署 |

完整输出保存于 `delivery_evidence/baseline_pytest.log`、`final_pytest.log`、`targeted_acceptance.log`、`remaining_failures.txt`。本环境Python3.13.5、pandas2.2.3、numpy2.3.5；Parquet实现缺失。依赖安装尝试受网络失败阻断。补齐pyarrow、duckdb等项目声明依赖后仍需**重新跑完整测试**，不能推断当前阻断之后的其他断言必然通过。没有把生产Parquet改成另一种格式或给旧失败测试添加skip来伪造绿灯。

两个新增编排测试显式将to_parquet替换为空操作，仅用于隔离校验筛选/编排逻辑；报告明确不含Parquet序列化验收。其余价格观察、SQLite冻结/导入/原子性/报告用真实实现运行。

## 反例与端到端覆盖

共同池：负60/120动量且低于MA120的股票能进入资格池，主机械仍排除；缺共同池、重复代码、历史不足、负流动性、非有限特征、Top20与共同池不一致均拒绝。单独去MA的反例进入消融，但不改变主机械原序。

AI：三模式改变各自所选集合；随机有界排序反例检查每行位移上限；单模式/并行登记和不可中途改模式；假批次、伪造/跨股证据、遗漏/重复股票、额外执行字段、错误动作、重复JSON键、NaN、未来自报时间、实际迟到、同模式冲突等拒绝并保留失败。多模式原子性和并发首个有效提交测试通过；相同重试幂等；晚到仅回顾性存档；包/源/机械组篡改可检测。

失败：没有证据或全unknown不被统计成有效AI；超时有回退原因；输入证据损坏进入隔离区而不抹除机械研究；网页原始JSON校验及CSRF保护；旧写入被拒绝但历史可读。

价格/账户：D1不作为可执行收益、缺股价不重新加权、空槽固定现金、窗口未到期不能产生已知收益、后续取价失败不抹除已验证历史观察；相同股票多组观察缓存不会产生资金复用；完整编排daily-lab两次运行穿过冻结—导出—导入—观察—报告。主账户哨兵不变；真实runtime仅只读审计。

## 合成演示

`research-demo` 本次实际生成1个demo批次、5个机械/基准组加3个AI组，共8组，D1/D5/D20均走真实价格观察代码，**订单与成交为0**。三模式均至少改变一个Top5选择；每模式仅1/20候选有有效动作，因此示例报告同时显示95%逐股unknown，不能被100%批次格式通过率掩盖。该示例没有实际调用AI，也没有真实股票收益含义。

## 四条路径的完成状态

| 路径 | 本次软件状态 | 真实证据状态 |
|---|---|---|
| 研究工具 | 共同池、有限实验、固定模式、可信提交与报告已实现 | 完整交易资格及原机器部署未认证 |
| 前瞻价格观察 | 8组端到端用合成样本通过 | 新版真实冻结/真实及时AI/到期样本尚无；旧库1冻结0观察保留原身份 |
| 连续模拟账户 | 原引擎及风控保留；不改核心执行指纹 | 新实验账户适配未开展；不能报告其净值、净收益、回撤和夏普 |
| 人工条件计划 | 原主代码保持不变，相关可运行回归通过 | Parquet阻断部分全量路径；无用户真实账户操作验收 |

结论只到软件逻辑和有限环境验证。机械有效性、AI额外价值都仍是待检验问题。允许未来得到没有改善或证据不足；本次不产生胜出策略，不自动替换主策略。

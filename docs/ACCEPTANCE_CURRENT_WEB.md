# 当前研究与网页验收（2026-09-10）

本轮增加本地网页、独立当前研究服务、客户端级网络路由、双源当日核验、原始响应版本及可追溯报告。没有放宽 v0.1.1 历史 PIT 回测约束，没有实盘或 AI 新闻决策。

## 真实数据

- 网页三股样本：`runtime/web-runs/20260910-232232-abb80f23/`，3 只尝试，1 只通过、2 只按策略排除，状态 complete。只验证软件功能，不构成个股推荐。
- 主板目录：`runtime/web-runs/20260910-233015-15c4f0b3/`，日期 2026-09-10，3195 只全部尝试，324 只通过，输出 Top 20；226 只数据问题，因此状态 partial。
- 数据问题包括：209 只最近 251 个基准日期有缺口；8 只未更新至本次日期；7 只双源名称不一致；2 只缺少前复权序列。没有将这些股票静默视为普通策略淘汰。
- 该目录只是新浪返回范围，不宣称交易所主板全集；今日研究也不证明历史无幸存者偏差。

本轮完整目录运行时使用了最终筛选规则及边界校验，之后仅调整报告文件原子发布方式与网页任务管理。报告保存其运行开始时的代码指纹，未将旧运行伪装成最终文件版本。

## 自动验证

原完整测试运行 96 通过、2 失败；两项失败是测试错误要求 benchmark 也必须具有名称。修正测试契约为“parser 可返回空名称，股票 verify_current 必须拒绝”，对应文件 21 项通过。

最终针对本轮数据与网页运行：`pytest -q tests/test_current_screen.py tests/test_webapp.py`，28 项通过。覆盖单位、缺失日期、未来/旧报价、空名称、低流动性不吞数据错误、异常响应、缓存收盘门槛、CSRF/Origin、目录穿越、恢复门禁、端口冲突、取消结果隐藏。

CSRF 拒绝请求测试额外连续 5 次通过；Windows Job Object 真实子进程（原定休眠 30 秒）关闭后在 5 秒内退出。`ruff check src scripts tests` 通过。未把初次失败报告为通过；完整历史长测试未在仅修改新模块后重复运行。

## 人工网页验证

已在浏览器确认新版布局、真实三股任务提交及结果显示、百分比和亿元单位、主板运行进度、刷新恢复历史结果、日志折叠与可用取消按钮。启动器使用本机 pythonw 后台服务，网页和服务来源均为 localhost；不显示 Token。

最终交互补充：真实接口诊断 `20260910-234036-5be04696` 成功；演示任务 `20260910-234130-d15b3fef` 经网页取消，随后 `20260910-234210-9e104819` 成功完成 70 个合成交易日并提供报告。重复启动正常复用本机服务。真实目录 HTML 和 CSV 下载均返回 200，CSV 核验包含 20 条候选。

限制：外部免费行情接口可能变化或中断；休市日不生成新的今日报告；缺行情保守排除；当前名称风险过滤不等于完整官方风险状态。没有新闻时间泄漏的联网验收，因为新闻功能未启用。

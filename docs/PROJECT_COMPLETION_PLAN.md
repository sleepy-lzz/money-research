> 版本说明（2026-09-16）：本文保留旧版历史设计/运行描述。当前研究流程、AI导入与完成状态以 [RESEARCH_V2_RUNBOOK.md](RESEARCH_V2_RUNBOOK.md) 和 [RESEARCH_V2_ACCEPTANCE.md](RESEARCH_V2_ACCEPTANCE.md) 为准。旧AI写入已停用，旧批次不转为新前瞻样本。

# 项目完成计划与持续改进

## 2026年9月15日状态更正

当前账本实际只有2026-09-11的1个冻结批次，尚无价格观察记录，网页 review_imports 为0。下文历史验收中的“0个冻结日”是当时状态，不代表今天。未找到9月14日完整运行记录，不能宣称该日已自动复盘完成或补写该日候选。

本次优先补连续运行审计：run-state.json 与 latest-run.json 记录阶段、实际源码路径和哈希、进程及时间；持有独占锁时识别上次未正常结束的记录。提前返回也保留 runner_hash。未改变机械、风控、AI权重或个人账户。

60日只是研究观察检查点，120日也不保证实盘可用；教学文档已更正“固定天数即通过”的暗示。严格 Snapshot、真实执行验证和独立研究验收仍未完成。

更新于 2026-09-11。任务是完成可用、可复核的 A 股研究与模拟工具，而不是无限增加指标。每天 16:30 的本地任务推进有明确验收条件的工作；需要真实交易日积累的结果如实等待，不能补造历史样本或宣布收益验证成功。

## 当前事实

- 社区适配已完成原始缓存复核、严格代码格式与市场/别名冲突检查、警告聚合及逐股票分类计数。见 [社区适配复核](COMMUNITY_ADAPTER_REVIEW_2026-09-11.md)。今天114条不匹配源于其他证券/栏目或缺码，不能放宽规则纳入；此前“前缀格式为主因”的推断已更正。

- 已有历史回测引擎、机械选股、当前行情交叉核验、网页工作台、资金/持仓输入、条件买卖价与数量、止盈止损/持有期限复核、新闻与股吧材料采集。
- 有每日固定四组候选冻结及 D+1/5/20 价格观察；另外新增独立虚拟资金连续账本、只读预检和网页入口。价格观察仍不等于模拟成交收益；真实可执行 Snapshot 尚未验收。
- 全量检查在本次修改前为 146 passed。新增修复的最终验证另记下方，不能沿用旧测试次数作为新版本通过证据。
- 还没有有效的真实前瞻冻结日；旧日期不会回填。真实严格历史回测仍受退市全集、状态记录和可见时间证明限制。
- AI 研究与社交内容目前主要用于解释、风险线索和独立对照，不能宣传已经证明增加 Alpha。缺少账户资金/持仓时，不伪造真实可买数量。

独立评审见 [2026-09-11 审查报告](PROJECT_REVIEW_2026-09-11.md)。报告是初查证据，以下验收记录才说明修复是否完成。

## 完成标准与优先顺序

| 优先级 / 里程碑 | 完成条件 | 当前状态 |
| --- | --- | --- |
| P0 本轮数据和计划修复 | 选股失败不阻断旧样本观察；规则/参数/证据派生版本独立；日历缺日不顺延；旧代码和旧账户计划不继续展示可执行数量 | 已实施，190项全量测试通过；账户完整性另列下一优先 |
| P1 连续运行和可恢复 | 首次真实冻结及随后到期结果成功；休市/缺数据/重启/重复运行有记录；源码与依赖能恢复到冻结版本；网页和后台版本一致且入口可用 | 等真实收盘，恢复/发布流程待补 |
| P1 连续模拟账户 | 与用户手工账户隔离；D 日冻结委托条件、D+1 保守成交或拒绝；T+1/整手/成本/公司行为/未知状态闭环；持仓连续更新；完整 audit、现金与净值可复现 | 软件入口与契约验收已实现；真实数据前瞻推进仍待验收 |
| P2 历史可信度 | 退市和暂停上市历史库存完整；session spine 与停牌证据闭环；业务日期/发布时间/实际获知时间分离；字段和 request 级完整性及版本冲突检查有验收数据 | 保留 fail closed，外部数据证明不足时不承诺完整回测 |
| P2 研究对照 | 新因子/新闻/社区建议先登记假设、固定规则、样本与失败条件；相同时间环境与覆盖率对照；计入无法成交、成本、相关性和多次试验选择偏差 | 保留机械基线，先积累；不每日调参追涨 |

软件完成指前四类工具行为和验收通过；策略是否有效是持续、独立的研究问题，不能以“所有测试通过”替代。真实下单不属于本轮完成标准。软件达标后自动任务继续维护与观察，不强行为了“持续改进”改动稳定代码。

## 主动审查和执行流程

1. 每日检查最新真实运行及遗留待办，先完成数据与软件必要检查；分别报告完成、已验证、待验证。
2. 周五、重大缺陷或关键变更前，由 `gpt-6-astra / xhigh` 独立审查。普通步骤保持默认思考设置；明确独立任务可用已授权 Luna 协作。
3. 审查先检查本项目和反例，再针对缺口查官方文档、GitHub、论坛和公开社交平台。每条候选记录出处、发布时间、与 A 股的适用差异、可复现证据、反对证据和预期代价。无法读取原帖时明确“未核验”，不能编造社区共识。
4. 主代理分类为接受、拒绝、有条件实验、数据不足待办，附理由。可逆软件修复自主实施；策略与风险关键改变需独立关键复核。不给每条网络观点照单全收。
5. 做适当测试、复核真实输入路径，记录改动和验收；有意义的进展、重要失败或需要用户行动才通知。没有新依据时保留固定版本，不重复跑同一回测挑最好收益。

## 社交平台参考边界

雪球、股吧、Reddit 等可以提供数据接口经验、交易规则边界、事件解释和研究假设。其样本存在选发、删帖、重复转发、机器人和推广偏差。社区热度不是独立事实来源；当前词典评分不是完整语言理解。

开发过程中看到的帖子只进入研发记录。想用于未来选股时另做前瞻 ingestion：固定原文 URL、evidence_id、原始发布时间及精度、实际 fetched_at/available_at、content_hash、采集覆盖与版本；删改记录保留修订。不可信时间或未来时间不能进入历史 replay，LLM 不能修改时间或凭空补证据。新帖子不能反向补入过去冻结批次。

## 后续候选队列

按最新独立报告复核后取舍，而不是一律实现：

- 日历覆盖临近年末时检查交易所新年度公告；未覆盖年份保持未知，不用通用工作日代替。
- **账户快照完整性门禁已完成。** 独立人工确认绑定资金、全部持仓、服务端时间与账户 revision；同日收盘市值与现金核对固定 1 元容差。未知估值不以成本兜底，冻结资金/其他资产风险未建模时不新开仓。此门禁提高人工资料一致性，不是券商自动核验或绝对证明没有漏录。
- 绩效独立调用时的基准缺日语义；本轮已修复 Snapshot benchmark 日历覆盖及当日市场状态新鲜度。
- 计划提交一致性和风险名触发本轮已修；后续补资金/仓位完整性及实际成交后的手工同步。
- 可恢复源码/配置/依赖快照；计划规则哈希应能找到确切实现，哈希本身不是源码备份。
- 个人持仓输入、执行后的手工同步与连续模拟账户的清晰区别；软件能持续判断不等于知道券商实际成交。
- 社区采样质量与新闻实体对应、来源层级、缺失覆盖；先做独立数据质量实验，再考虑改变权重。

## 本轮验收

- 自动化 `a` 已用 Codex 工具更新为工作日北京时间 16:30，名称“A股每日检验与持续改进”，纳入独立审查、公开社交参考、主代理取舍和实现流程。
- 全量修改前基线：146 passed / 109.60s。
- 最终版本全量测试：**190 passed / 115.07s**；`ruff check src scripts tests` 通过。新增反例涵盖阶段失败隔离、日历漏日/假日/跨年、显式来源时间与完整性、参数/源码版本、旧计划/旧screen缓存、账户CAS、ST退出触发与同代码旧归档保护。
- 实际执行 `daily-lab --check-only`：**105 passed / 9.22s**，完整日志与报告保存在 `runtime/daily-lab/20260911-013502-c9f5ad14/`。状态为 deferred，selection/observation 均 not_run，真实冻结样本仍为0；软件检查与真实采集明确区分。
- F01–F05 已选取实施；补充堵住 planner 使用同日旧版 screen 缓存重贴当前规则哈希的入口。F12 的过期股票范围/流动性说明和用途对照已修正文档；暂未做自动生成的统一规格系统。
- 真实接口日期探针：`runtime/calendar-probes/20260911-012924-be9b862d/`，2026-09-11 实际检查腾讯指数640行，最后市场日期09-10，独立日历最近251日完全匹配、缺日0。原始来源时间见receipts，未冻结旧信号；这不证明全市场覆盖、历史PIT或策略收益。
- 上轮结束时 F06账户完整性、F07真实PIT材料、F08开盘/真实成交证据、F09永久未知重试终态、F10完整实验登记和F11可恢复源码版本列为待办；本次 F06 进展见下方新增验收。

## 本次继续开发验收

- F06 已实现：严格账户确认 API、SQLite CAS、资金/持仓变化失效、同日收盘对账、未知值保留 null、新仓数量门禁；持仓退出触发保持可见。网页表单必须重新勾选，未保存资金不能确认，后台 revision 改变会取消旧勾选。
- 新增 `paper_workspace.py`、`paper-account` CLI 和 `/paper` 网页。虚拟资金从零持仓开始；状态查询不创建账本，预检只处理临时 SQLite 副本。正式前瞻推进有账户独占锁、完整重验、session 与 payload 同事务、日归档、报告失败恢复、漏日及版本变化终止状态。
- Astra / xhigh 有限独立复核已完成。实修旧 CLI 绕过账户所有权（包括异工作目录、绝对路径、未创建账本），漏日误显示为等待数据，以及费用/含滑点成交价标签。未发现新的关键资金、时间或重复成交缺陷；这是代码复核结论，不是实盘结论。
- 全量回归 **238 passed / 128.57s**。随后补充一键启动识别旧后台版本的兼容修复，相关网页测试 **14 passed / 6.79s**；`ruff check src scripts tests` 通过，planner/paper 页面 JavaScript 语法检查通过。未进行浏览器视觉验收，不将其记为通过。
- 实际运行 `daily-lab --check-only`：**149 passed / 17.40s**，报告目录 `runtime/daily-lab/20260911-090420-d1a4ce26/`；状态 deferred，没有冻结历史或启动真实撮合。只读 `paper-account list` 返回账户为空，快照仅 `demo-320-42` 与 `demo-v2-320-42`。
- 一键入口遇到旧版本占用默认端口时，在新端口启动当前后台；不结束旧进程。此分支以临时测试对象验证，没有操作用户已运行的服务。
- 尚未建立用户模拟账户、假定用户资金或开始真实模拟交易；实际虚拟金额由用户在网页选择。已有自动任务继续每日价格观察与软件检查，新增账户测试已纳入。每日模拟自动推进还需可信数据适配及账户绑定，不能声称已经无人值守运行。
- 下一优先：获得并验收最小真实前瞻 Snapshot（库存/交易状态/可见时间/开盘证据），在公开数据不足处明确等待；同时积累真实冻结样本，补源码/依赖恢复档案。保留固定机械策略，不用新增指标代替数据验收。

## 盘中条件监控增量（2026-09-11）

- 已新增 `/intraday` 网页及显式启动/停止接口，交易日连续竞价时约每 30 秒采集新浪/腾讯双源报价（另加请求耗时）。记录实际获取时间、双源时间、原始回执和哈希；暂停、缺源、冲突、过期或盘口不足时不提供相应可执行数量。免费报价不能证明成交或完整交易状态。
- 监测有效收盘计划的买入区间、持仓止盈止损和持有期限。按当前手工现金、持仓市值、风险与原计划数量上限计算；原计划 0 股不转正，同一次确认首批正买入分配固定上限，不跨轮重复分配资金。T+1/实际可卖数为零仍保留退出触发提醒。
- 手工账户确认绑定当日、输入哈希与 revision；检查发布前再次核验账户、计划、规则和停止状态。实际成交后用户须同步现金/持仓并重新确认，系统不读取券商账户，也不下单。收盘计划有效期统一到下一个实际交易日 16:00，覆盖周末与节假日。
- 网页轮询、独立到期计时器、失联清除、提醒去重和批次身份校验已实现。用户授权后可显示页面存续期间的浏览器提醒；关页不保证通知，退出后台后停止监控，重启不会自动恢复。新闻仍沿用收盘证据，不是实时新闻判断。
- Luna 完成行情适配与独立网页工作；`gpt-6-astra / xhigh` 完成关键只读复核。发现并修复了报价采集截止时间、盘口/风险状态一致性、跨轮资金分配，以及无计划持仓提醒和网页独立过期清除问题。
- 真实两股票双源探针成功：`runtime/intraday-probes/20260911-1006-dual-source/` 保存原始数据及回执。一次较早新浪请求失败时保持缺失，之后重新请求成功；没有放宽双源要求。仅证明本次连通性与解析，不证明策略收益、全市场、历史 PIT 或成交。
- 已做早期页面视觉检查；最终页面重新视觉检查时浏览器工具连接失败，因此最终视觉复查未完成。页面身份/过期逻辑测试通过；没有启动用户账户监控、发送系统通知或生成真实订单。
- 最终代码全量回归：**277 passed / 175.94s**，`ruff check src scripts tests` 全部通过。包含盘中时间与报价、T+1/数量预算、账户/停止/计划竞态、HTTP 契约和实际页面 JavaScript 提醒身份及过期测试；这些软件验证不代替真实前瞻策略验收。
- 日常自动检查已纳入盘中后端和双源行情回归。下一步仍是验收真实前瞻数据和积累固定规则样本，不因增加盘中提醒而宣称策略已经验证。

## 国内外事件与财务摘要增量（2026-09-11）

- 新增 `market_context.py`：五类国内外宏观主题聚合检索和美联储货币政策 RSS，有限近 7 日样本，来源分级、时区、获取回执、内容哈希、主题命中去重和正反条件情景。当前没有公司暴露映射或事实核验，不将新闻自动断言为股票利好/利空。
- 新增 `financial_context.py`：对候选及全部持仓逐股读取东财主要财务指标摘要，每股最多展示 4 期，保留日精度公告/修订日、实际获取及可见时间。营收/归母/扣非、每股经营现金流、ROE/负债率/同比使用维护方文档口径；缺失保留未知，同期冲突和错码/未来日期拒绝，不回退旧报告冒充最新。
- 两层单独冻结进计划与输入哈希，在持续计划网页显示，交易权重为 0，不流入原个股新闻证据、排名、股数或止盈止损。更新后台版本为 6，一键入口可识别。财报尚非完整三大报表、原始公告和审计意见核验；尚无历史 PIT 财务数据和实时新闻通知。
- `gpt-6-astra / xhigh` 完成独立代码复核及另一路因子研究，见 [研究报告](FACTOR_RESEARCH_2026-09-11.md)。采纳财报事实优先/宏观情景/社区零交易权重与独立对照方法；机械财务 90/10、机械新闻 95/5 仅候选实验，数据未达标，不实施加权策略或每日调参。
- 根据审查修复公共新闻解析器把紧凑日期/ISO周日期/无时刻偏移误解释为精确时间的问题，新增反例。RSS 官方货币主题按固定频道识别，旧公告不会因缺少标题词被误标为解析失败。
- 实测目录 `runtime/context-probes/20260911-104959/`：五类聚合新闻 25 条；美联储请求成功、近7日材料0条；600519.SH/000001.SZ各4期财务摘要，共8期。每个请求原文和回执保留；仅验证此次接口与解析，不证明原始披露事实、全市场覆盖、历史版本完整或策略收益。
- 最终全量测试 **315 passed / 189.05s**；`ruff check src scripts tests` 通过。新增数据日期/时区/缺失/冲突、页面实际 JS 渲染函数，以及注入不同宏观与亏损财务数据不改变原计划数学的集成反例。未进行本次网页浏览器视觉验收，不记为通过。
- 日常软件检查已纳入宏观和财务适配器测试；真实生产收盘复核会采集两层参考，未在盘中伪造新的收盘计划。后续优先：原始公告核验与财务覆盖审计、已知预约披露版本；财务 TT​​M/行业可比与真实前瞻 Snapshot 条件未满足前不启动研究报告中的收益实验。
